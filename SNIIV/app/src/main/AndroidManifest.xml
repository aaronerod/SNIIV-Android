<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.gob.conavi.sniiv" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="Menú" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReporteGeneralActivity"
            android:label="Reporte General">

        </activity>

        <activity android:name=".OfertaActivity"
            android:label="Oferta">

        </activity>

        <activity android:name=".DemandaActivity"
            android:label="Demanda">

        </activity>
    </application>

</manifest>
